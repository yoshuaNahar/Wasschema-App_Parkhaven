Provide services that are required for the app to function correctly.
Preferably don't declare components. Only if they are used inside the app component.

The core module needs to be imported inside the app module.

The core  module should never be lazily loaded.
